---
const {title, icon, description} = Astro.props;
---
<div class="card">
    <div class="card-header">
      <i class="fas" {icon}></i>
      <h2>{title}</h2>
    </div>
    <div class="card-body">
      <p>{description}</p>
    </div>
</div>